import { AdvStatusPipe } from './adv-status.pipe';

describe('AdvStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new AdvStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
